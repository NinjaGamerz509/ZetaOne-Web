Navigation components (Navbar, Footer, MobileMenu, SearchModal) live in
`components/layout/`. Documentation-specific navigation (sidebar, table of
contents) lives in `components/sections/documentation/`.

They are not duplicated here to avoid two sources of truth for the same UI.
