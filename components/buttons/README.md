The shared Button component lives at `components/ui/Button.tsx` and is used
across the entire site (primary/secondary/tertiary variants, sizes, icons).

It is not duplicated here to avoid two sources of truth for the same UI.
